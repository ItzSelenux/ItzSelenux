set -g default-terminal "screen-256color"

set -g status-bg '#7abf33'
set -g status-fg '#2a2a2a'

unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

set-option -g status-right "%I:%M %p"
set-option -g status-left ''
set -g mouse on
